version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
hooks:
  BeforeInstall:
    - location: deployment/scripts/stop.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: deployment/scripts/start.sh
      timeout: 300
      runas: root

  
